#!/bin/sh
cd ${0%/*} || exit 1    # run from this directory
set -x

wclean $makeType intermediate
wclean $makeType spray

# ----------------------------------------------------------------- end-of-file
